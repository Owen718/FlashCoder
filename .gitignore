# Local secrets
.env
.env.*
!.env.example

# Local runtime state
.dsh/

# Node build and package artifacts
node_modules/
dist/
*.tgz

# macOS metadata
.DS_Store

# Internal design notes and plans, not part of the published source
# The internal repository (Owen718/FlashCoder-dev), checked out here so
# code and notes share one directory tree. It has its own .git; this one
# must never see inside it.
internal/
dev/
plans/
AGENTS.md
PLANS.md

# Release artifacts
release/

# bash children run with HOME set to the workspace, so anything that writes to
# $HOME lands here. That is how .npm/ reached this repository in the first place.
.ipython/
.local/
.prime/
Library/
package-lock.json
.npm/

iphone-buying-guide-2026-08.md
.zsh_history
.config/
